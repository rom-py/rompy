# Requirements for the repository splitting script
# Install with: pip install -r split_requirements.txt

# Core dependencies
PyYAML>=6.0
git-filter-repo>=2.38.0

# Optional dependencies for TOML file handling
tomli>=2.0.0; python_version < "3.11"
tomli-w>=1.0.0

# Development and testing (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
